import { style } from "@vanilla-extract/css"

const styles = {
    conatiner: style({
        width: "75%",
        height: "100vh",
        margin: "20px auto",
        textAlign: "center",
    }),
}

export default styles
