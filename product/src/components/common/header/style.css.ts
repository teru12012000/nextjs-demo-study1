import { style } from "@vanilla-extract/css"

const styles = {
    header: style({
        backgroundColor: "black",
        color: "white",
        padding: "10px",
    }),
    h1: style({
        fontSize: "30px",
        fontWeight: "bold",
    }),
}

export default styles
